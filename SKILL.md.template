# memory-bank

AgentMemory 持久记忆银行 v2 — 跨会话、跨 Agent 的记忆库（pi / Claude Code / CodeBuddy 共用同一套）。

## 何时使用

- 用户提到"记忆""上次""以前做过"或需要跨会话上下文时
- **会话中自动捕获**：完成重要工作、获得新知识、踩坑、被用户纠正、发现重复模式时——**不要问用户，直接 capture**（Q2：完全自动 + 安全网兜底）
- 开始新项目/接触新领域时，先检索是否已有相关记忆

## 位置与命令

记忆库根目录：`<MEMORY_ROOT>`

优先使用 daemon（快速，~10ms）；daemon 未启动时降级为 CLI：

```bash
# daemon 方式（常驻服务，消灭 1.3s 进程启动开销；ADR-0004）
curl -s -X POST http://127.0.0.1:8123/memory -H "Content-Type: application/json" \
  -d '{"cmd":"add","body":"...","auto":true}'
curl -s "http://127.0.0.1:8123/memory" ...  # search 见下

# CLI 方式（离线兜底，同一套语义）
python <MEMORY_ROOT>/scripts/memory_tool.py <命令> [参数]
```

启动 daemon：`python <MEMORY_ROOT>/scripts/daemon.py`（可选 `--with-bridge` 启用飞书桥）。

## 常用命令（v2）

| 命令 | 用法 | 说明 |
|------|------|------|
| **capture** | `capture --body "内容"` 或 `capture --title "T" --body "B"` | **自动捕获**：永不交互，type/category/tags 全自动推断，写前敏感拦截自动标 secret |
| **add（极简）** | `add "一句话"`（stdin 或 --body） | 极简写入：title=首句、type/category/tags 自动推断；低置信且手动时确认一次 |
| add（完整） | `--title --tags --category --type --confidence --secret` | 显式指定全部字段 |
| search | `search "关键词"` | 本地 FTS 检索（零 token）；支持 `--category/--tag/--limit/--json` |
| **search --synthesize** | `search "问题" --synthesize` | LLM 综合回答（冷路径）：每条结论带 `[来源:bank/...]`，无记录明说"无记录" |
| get | `get <path>` | 读全文；secret 需 `--force`；**刷新 last_accessed（双信号读取侧）** |
| update | `update <path> --type fact --pin true --invalidate --supersede bank/xxx.md` | 改类型/钉住常驻/标记失效/建立沿革链 |
| archive | `archive <path>` | 归档（不出现在检索与 PRELOAD） |
| list | `list --category knowledge` | 按分类列出 |
| health | `health` | 健康报告（条目/类型分布/敏感标记） |

## 条目类型（type）— 冲突处理立场（ADR-0001）

- `fact` — 外部可验证（规范、数据、工具用法）→ 冲突时系统附外部依据，**由用户拍板**
- `belief` — 你的理解/方法论 → 不纠正，新理解自动 `superseded_by` 保留沿革
- `preference` — 喜好习惯 → 永不纠正、永不触发冲突检测

写入时自动推断；`update --type` 可改。

## 分类（category）

- `user` — 用户身份、角色、偏好
- `projects` — 项目上下文
- `knowledge` — 领域知识、方法论、工具链
- `lessons/failures` — 踩坑记录（现象/根因/解决）
- `lessons/corrections` — 被纠正的教训（原错误理解/正确理解）
- `lessons/patterns` — 重复模式（skill 候选）

## 会话启动：PRELOAD 预热

```bash
# 启动时先读预热索引（≤200 行：pinned 常驻 + 近 7 天；全文按需 get）
python <MEMORY_ROOT>/scripts/build_preload.py
# 然后读取 <MEMORY_ROOT>/PRELOAD.md
```

预热规则：pinned 常驻层永不被挤出；secret 条目永不进 PRELOAD（ADR-0002）。

## 安全规则（三道防线，ADR-0002）

1. **写前拦截①**：凭证类（sk- 密钥/AKIA/ghp_/私钥/Bearer）→ **拒绝写入**；敏感类（内网 IP/手机号/邮箱/Cookie）→ **自动标 secret**（摘要隐藏、get 需 --force、不发给 LLM、不进 PRELOAD）
2. **周扫补检②**：consolidate --mode llm 对近 7 天条目补扫，漏网的自动标 secret
3. **提交闸门③**：git pre-commit 全量扫描，Block 级命中拒绝提交
4. `.env` 含 LLM key：永不读取、输出或提交

## 维护

- 每周整理：`python <MEMORY_ROOT>/scripts/consolidate.py --mode auto`（规则：双信号衰减/去重/模式统计，零成本）
- 可选增强：`python <MEMORY_ROOT>/scripts/consolidate.py --mode llm`（LLM 提炼 + 语义冲突 → CONFLICTS.md 待你决策 + 类型复查 + **CANDIDATES → candidates/ SKILL.md 草稿**；需 `.env` 配 `DEEPSEEK_API_KEY`）
- 冲突卡在 `bank/CONFLICTS.md`，**由用户拍板**（系统只给依据不给判决）
- SKILL 草稿在 `bank/candidates/<name>/SKILL.md`，审阅后可启用

## 注意

- 写之前先 `search`，避免重复添加同一主题
- 跨 Agent 共用时写入即共享；并发写请错峰（consolidate 固定每周一次即可）
- 记忆是"事实+经验"，不是任务待办；任务状态不要写入
- **热路径零 LLM**（ADR-0005）：add/search/get 永远不调用 LLM；synthesize 与 consolidate 是用户主动/每周触发
